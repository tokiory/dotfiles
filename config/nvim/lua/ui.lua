-- Licensed under MIT
-- 2024, Daniil Shilo (tokiory)

-- HACK: vitesse doesn't work on initial loading 'cause colorbuddy, so we load gruvbuddy first
vim.cmd("color gruvbuddy") 
vim.cmd("color vitesse")
